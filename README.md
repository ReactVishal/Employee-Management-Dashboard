# Employee-Management-Dashboard

## 📌 Project Overview
The Employee Management Dashboard is a React-based web application designed to manage employee records in a simple, intuitive interface.  
It provides features such as:
- User authentication (mock login)
- Dashboard with summary cards (total, active, inactive employees)
- Employee CRUD operations (create, read, update, delete)
- Search and filter employees by name, gender, and status
- Image upload for employee profiles
- Print functionality for reports
- LocalStorage persistence (with optional API service integration)

This project was built as part of an assignment to demonstrate frontend architecture, modular code organization, and maintainable design.

---

## 🛠 Tech Stack Used
- **React 18** – UI library for building components
- **React Router v6** – Client-side routing
- **Parcel** – Bundler for fast development and build
- **JavaScript (ES6+)** – Core language
- **CSS (custom styles)** – Styling and layout
- **LocalStorage / Mock API** – Data persistence

---

## 🚀 Steps to Run the Project Locally

1. **Clone the repository**
   ```bash
   git clone https://github.com/your-username/employee-dashboard-project.git
   cd employee-dashboard-project
